# Azure File Share sync backend (optional — only needed for azure sync target)
AZURE_STORAGE_CONNECTION_STRING=

# AWS S3 sync backend (optional — boto3 also reads ~/.aws/credentials)
AWS_DEFAULT_REGION=ap-southeast-2
